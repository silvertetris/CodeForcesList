fun main() {
    val br = System.`in`.bufferedReader()
    val bw = System.out.bufferedWriter()
    val t = br.readLine().toInt()
    /*
    순서를 유지하면서 최대값을 뽑음 -> 그리디로 하는데 -> 문제는 배수구가 2개
    1. dp
    2. cartesian tree -> max-cartesian tree
     */
    repeat(t) {

    }
}