#include <bits/stdc++.h>

#define ll long long
#define ld long double

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin>>t;
    while(t--) {
        ll a;
        int n;
        cin >> a >> n;
        int d1, d2;
        cin >> d1 >> d2;
        int aLength = to_string(a).length();
        ll res = 2e18;

        for(int i = max(1, aLength-1); i<=aLength+1; i++) {
            
        }
    }
}