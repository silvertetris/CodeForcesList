#include <bits/stdc++.h>

#define ll long long
#define ld long double

using namespace std;

int main() {
    ios::sync_with_stdio(false);

    cin.tie(0);
    cout.tie(0);

    int t;
    cin>>t;
    /*
    1. 배열 개수는 무조건 홀수 2^k +1
    2. 사이사이 채우는식 -> 이분탐색 느낌으로 머지 소트 느낌으로
    3. 배열의 개수는 int 최대치를 안넘은 2^30+1
    
    */
    while(t--) {
        int n, k;
        cin>>n>>k;

        string s = "";
        string z = "";
        cin>>s>>z;

    }
}